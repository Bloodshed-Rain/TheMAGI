export function Library({ refreshKey: _ }: { refreshKey: number }) {
  // Full implementation lands in Task 19 of the new-UI plan.
  return (
    <div>
      <div className="page-header">
        <h1>Library</h1>
      </div>
    </div>
  );
}
